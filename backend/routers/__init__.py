"""API routers.

Business routers (auth, workspaces, documents, generation, chat, review,
history, analytics, search, catalogue, models) are added by their milestones.
The only router present at M0 is :mod:`backend.routers.health`.
"""
