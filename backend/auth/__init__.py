"""Authentication package (M1).

Endpoints, schemas, service and migrations for the account/session domain.
The domain implementation in ``src/`` stays untouched: auth is a FastAPI-layer
concern and lives entirely here.
"""
